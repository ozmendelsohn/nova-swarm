services:
  - type: web
    runtime: static
    name: nova-swarm
    plan: free
    buildCommand: echo "static site - no build"
    staticPublishPath: .
    autoDeploy: true
    headers:
      - path: /*
        name: X-Frame-Options
        value: DENY
