// Template for js/config.js — copy to js/config.js for LOCAL dev and fill in.
// js/config.js is git-ignored and is generated at deploy time from Render env
// vars (SB_URL / SB_ANON) by the buildCommand in render.yaml, so no keys are
// ever committed. The anon key is public-by-design; security comes from RLS.
window.SB_URL = '';   // e.g. 'https://yourproject.supabase.co'
window.SB_ANON = '';  // your project's anon/public API key
